<section class="goals">
    <img class="js-bg" src="{{root}}{{goals.bgImg}}" alt="Banner">
    <div class="container">
        <div class="row">
            <div class="goals__content">
                {% if goals.desc %}
                    <p class="goals__desc">{{goals.desc}}</p>
                {% endif %}
            </div>
        </div>
    </div>
</section>